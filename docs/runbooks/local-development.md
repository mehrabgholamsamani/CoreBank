# Local Development

Set no real credentials or customer data in this project. Start the simulated infrastructure with `docker compose up --build` and check `http://localhost:3010/health`, `http://localhost:3010/ready`, and `http://localhost:3010/openapi`. The stack must never be connected to real financial systems.
