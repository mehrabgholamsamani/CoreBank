receivers:
  otlp:
    protocols:
      http: {}
exporters:
  debug: {}
  otlphttp/tempo:
    endpoint: http://tempo:4318
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [debug, otlphttp/tempo]
